# User-scoped AI access

Future KOS AI capabilities will receive only explicit user-selected AI scope and will return drafts or proposed actions that require confirmation before altering Workspace data. KOS will not silently transmit or index an entire Workspace with an external model provider.

# Desktop-first web scope

KOS v1 will be a responsive Next.js web application optimized for desktop productivity, using the reference design's persistent navigation and constrained canvas. It will not promise offline synchronization, local-only storage, or packaged native desktop delivery; those require separate product and synchronization decisions.
